# Source xlsx is huge (1.7 GB) — don't commit, the extracted JSON + images are the deliverable
诺贝尔-瓷砖高清素材图+实铺效果图.xlsx
*.xlsx

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Secrets (Mimo key lives in localStorage on the device, not in source)
.env
.env.local

# Claude Code local config (preview launch settings, etc)
.claude/

# Scraper working state (queue, progress files, partial downloads)
tmp_scrape/
scrape_progress.json
scrape_log.txt

# Runtime-generated by entrypoint.sh on Zeabur — never commit (would leak key)
docs/config.js
